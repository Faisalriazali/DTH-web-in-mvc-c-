@model IEnumerable<R_DTH.Models.package>

@{
    ViewBag.Title = "Index";
}

    <!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        Package List
        <small>Admin View</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i>Package</a></li>
        <li><a href="#">List</a></li>
        <li class="active">View</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">


            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">
                            @Html.ActionLink("Create New Package", "Create")

                    </h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Package Name</th>
                                <th>Package Charges</th>
                                <th>Package Channel</th>
                                <th>Package Details</th>
                                <th>Take Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.pack_name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.pack_charges)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.pack_channel)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.pack_details)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.pack_id }) |
                                        @*@Html.ActionLink("Details", "Details", new { id = item.pack_id }) |*@
                                        @Html.ActionLink("Delete", "Delete", new { id = item.pack_id })
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Package Name</th>
                                <th>Package Charges</th>
                                <th>Package Channel</th>
                                <th>Package Details</th>
                                <th>Take Action</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pack_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pack_details)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pack_charges)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pack_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pack_details)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pack_charges)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pack_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.pack_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pack_id })
        </td>
    </tr>
}

</table>*@
