@model IEnumerable<R_DTH.Models.dealer>

@{
    ViewBag.Title = "Index";
}

<section class="content-header">
    <h1>
        Package List
        <small>Admin View</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i>Package</a></li>
        <li><a href="#">List</a></li>
        <li class="active">View</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">


            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">
                        @Html.ActionLink("Register New Dealer", "Create")
                    </h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Contact #</th>
                                <th>Email</th>
                                <th>Total Paid</th>
                                <th>Amount to Paid</th>
                                <th>Take Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dealer_name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dealer_contact_no)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dealer_email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dealer_totalypaid)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dealer_payment_left)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.dealer_id }) |
                                        @Html.ActionLink("Details", "Details", new { id = item.dealer_id }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.dealer_id })
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Name</th>
                                <th>Contact #</th>
                                <th>Email</th>
                                <th>Total Paid</th>
                                <th>Amount to Paid</th>
                                <th>Take Action</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dealer_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_contact_no)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_totalypaid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dealer_payment_left)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_contact_no)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_totalypaid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dealer_payment_left)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.dealer_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.dealer_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.dealer_id })
        </td>
    </tr>
}

</table>*@
